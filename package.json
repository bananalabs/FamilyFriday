{
  "name": "family-friday",
  "version": "1.0.0",
  "description": "Family Friday",
  "scripts": {
    "install": "cd client; npm install; cd ../server; npm install; cd ..",
    "server": "node --harmony ./server/app.js &",
    "client": "cd ./client; npm run build:dev; npm run dev",
    "start": "npm run server; npm run client"
  }
}
